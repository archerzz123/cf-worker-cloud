1. 使用英文思考和搜索查询, 使用中文输出.
2. 原子化, 杜绝相同的代码/业务逻辑/算法/数据结构/字符串/其他硬编码在两个及以上地方出现, 尽量避免重复逻辑(DRY 原则), 写新函数之前, 可以先做两件事, 2.1 先搜一下项目里有没有类似逻辑, 2.2 如果有类似实现，看看能不能: 直接复用 or 抽成一个公共函数 or 稍微拆分一下已有代码. 示例: 比如 `A函数` 和 `B函数` 里都有一段处理 XXX 的逻辑, 其实可以抽成一个 processXXX() 的方法, 这样两个地方都可以调用
3. 保持单一文件结构, 后端代码写在 worker.js 文件里, 前端代码写在 public/index.html 里, 测试代码写在 worker.test.js 里
4. worker.js 技术架构, ts + vercel ai sdk + cloudflare d1 + zod, 底层使用 vercel ai sdk 完成各平台各接口的AI请求和封装
5. public/index.html 技术架构, vue cdn + tailwindcss + fetch + vercel ai sdk cdn
6. worker.test.js 测试用例完整覆盖所有场景
7. 先写测试用例, 再写代码
8. 常量化, 不要硬编码
9. 编写开发文档时, 必须要有用户用例图, 时序图, 流程图, 要有完整的数据结构和表结构以及对应的注释, 要有有全部完整的函数签名, 要有全部完整的API输入输出说明, 要有伪代码示例. 这里的“全部”的意思是指, 后面开发接手时, 无须再新增任何函数, 即使是内部函数也要在文档中有体现出来, 开发只需要实现空函数即可.
10. 定义数据结构或变量时, 必须清晰的写明每个字段/变量的含义, 用法和边界等, 禁止模糊表达
11. 定义函数时, 必须清晰的写明函数的含义, 签名(参见 10), 异常/错误, 用法和边界等, 禁止模糊表达
12. 定义 API 时, 参见 10, 11
13. 严格遵守用户的需求, 不减少也绝不未经同意添加新的业务逻辑, 可适当的优化业务流程/逻辑
14. 连续对话中主动记忆并引用上下文，确保前后一致，避免重复、矛盾、脱节
15. 充分考虑时效性与上下文一致性，避免前后矛盾与过时建议
16. 每次遇到新的需求或业务更改之前, 必须先更新开发文档, 再按开发文档进行开发
17. 当前更新代码或数据结构或数据类型时, 需要进行全链路追踪, 同步更新和优化业务
18. 当前涉及前端界面设计, 优化和开发时, 应先充分思考如何改动才是对用户体验最好的(比如如果你是一名真实用户的情况下, 你希望是什么样子的), 其次查找最适合本次改动的 skills, 最后开始执行任务